linters:
  enable:
    - gofmt
    - govet
    - errcheck
    - staticcheck
    - unused
    - gosimple

linters-settings:
  gofmt:
    simplify: true

issues:
  exclude-dirs:
    - vendor
    - internal/openapigen
